# 当前断点

> 会话开始 Claude 只读这一个文件定位进度。
> 学习记录 / 学习清单 / 学习总结 都按需触发式读取。

---

## 📌 路线版本

**v2 · 17 周精简版**(2026-04-26 修订,从原 28 周脱产版精简而来)

- 起止:2026-04-18 → 2026-08 月中下旬
- 节奏:每天 6h,空窗脱产
- 求职定位:中小型 AI 公司 + 创业公司 + 传统行业 AI 部门(大厂短期不投)
- 砍:微调 + 评测、Reflection/ToT/Prompt 攻防、裸写 ReAct → 改读源码
- 加:Prometheus/Grafana/locust/LiteLLM/AutoGen/CrewAI/Milvus 简介
- OnCall 项目:基于小林 Coding 教程"二创 + 生产化补足",4 周完成

---

## 📍 当前进度

- **当前阶段**:阶段一 · Python 高级语法补漏
- **当前周次**:Week 2(异步 + 工程化)
- **当前 Day**:Day 11 - 类型 + Pydantic v2(待开始)
- **状态**:Day 10 异步生态完成 ✅。`httpx.AsyncClient`(连接池 / 4 档超时 / `Limits`)、`aiofiles`(线程池包装,目的是不阻事件循环)、FastAPI 异步选型规则(`async def` 进事件循环 / `def` 进线程池)三块讲完。作业 100 文件下载跑通(Sem=10 + retry 2 + `gather(return_exceptions=True)` 兜异常 + 统计成功失败)。**踩坑大集合**:`sleep` 漏 await(第 3 次踩同款)/ `asyncio.run(main)` 漏括号 / f-string 变量没 `{}` 包 / `"ab"` append 模式重试损坏文件 / `r` 不是 bytes 要 `r.content` / `for url, i in urls` 解包错。/simplify 抓出真 bug `r.raise_for_status()` 缺失(4xx/5xx 当成功写入)+ 5 条结构性优化。**核心教学点**:Semaphore 包 retry 的 tradeoff(sleep 期间占槽 → 实际并发 < N;生产做法 retry 在外 + jitter 防雷暴)。
- **下次启动指令**:开始 Day 11 —— `typing` 模块(`Optional` / `Union` / `List` / `Dict` / `Literal` / `TypedDict` / `Protocol`)+ Pydantic v2 核心(`BaseModel` / `Field` / `field_validator` / `model_validator` / `ConfigDict`);作业是给 LLM 消息(role / content / metadata)写 Pydantic Schema,带校验器(role 限 user/assistant/system,content 不能空)。

---

## 🚨 卡壳问题清单

<!-- 格式:[日期] 问题描述 — 状态(待解决 / 已解决在 YYYY-MM-DD) -->

- [2026-04-21] 生成器 + `with open(...)` 的文件生命周期:放在生成器里的 `with` 不由 caller 决定关闭时机,要靠生成器耗尽 / GC / `.close()`。**暂不解决,等实际遇到资源泄漏场景再处理**(正确规避:把 open 交给调用者,生成器只接 file-like)。
- [2026-04-27] `Semaphore` 包 retry vs 包单次的 tradeoff —— 这次选了简单写法(包 retry),但生产要"retry 在外 + jitter"。**已知不改,作业场景 OK**;OnCall 项目里实操 LiteLLM/MCP 重试时再回来调。
