# 当前断点

> 会话开始 Claude 只读这一个文件定位进度。
> 学习记录 / 学习清单 / 学习总结 都按需触发式读取。

---

## 📍 当前进度

- **当前阶段**:阶段一 · Python 高级语法补漏
- **当前周次**:Week 2(异步 + 工程化)
- **当前 Day**:Day 10 - 异步生态(待开始)
- **状态**:Day 9 async 深入完成 ✅。`create_task` / `Semaphore`(甜点公式 `rate × 耗时 × 0.5`)/ `wait_for`(超时)/ `cancel`(三铁律)/ `Queue`(背压解耦)五块全部讲完。两个作业跑通:(1) 并发限流爬虫(30 URL + sem=3 + timeout=3 + 故意超时)6s 完成,1 失败 29 成功;(2) 告警流 Queue(10 alert + 3 consumer + maxsize=2)round-robin 完美。附带补了下游/线程池/503/解耦四个工程词汇。坑最深的是异常类型混(`httpx.TimeoutException` ≠ `asyncio.TimeoutError`)和**后台 task 异常静默丢失导致死锁**。async 基础核心心智到这里基本过关。
- **下次启动指令**:开始 Day 10 —— `httpx.AsyncClient` 深入(Client 配置、连接池参数、超时细粒度拆分 connect/read/write/pool)+ `aiofiles` 异步文件 I/O(本质是线程池包装,结合 Day 9 讲的"本地 1000 JSON"场景) + FastAPI 异步视角(路由里 `async def` vs `def` 的区别,sync endpoint 其实在线程池跑);作业是**异步下载 100 个文件**(要求:Semaphore 限流 + wait_for 超时 + aiofiles 写入 + 失败重试)。

---

## 🚨 卡壳问题清单

<!-- 格式:[日期] 问题描述 — 状态(待解决 / 已解决在 YYYY-MM-DD) -->

- [2026-04-21] 生成器 + `with open(...)` 的文件生命周期:放在生成器里的 `with` 不由 caller 决定关闭时机,要靠生成器耗尽 / GC / `.close()`。**暂不解决,等实际遇到资源泄漏场景再处理**(正确规避:把 open 交给调用者,生成器只接 file-like)。
