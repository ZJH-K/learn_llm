# 当前断点

> 会话开始 Claude 只读这一个文件定位进度。
> 学习记录 / 学习清单 / 学习总结 都按需触发式读取。

---

## 📍 当前进度

- **当前阶段**:阶段一 · Python 高级语法补漏
- **当前周次**:Week 2(异步 + 工程化)
- **当前 Day**:Day 8 - asyncio 基础(待开始)
- **状态**:Week 1 全部完成 ✅。Day 7 周复盘做完,10 道面试题暴露两个认知漏洞(`yield from` 接 return 值、`__eq__` 重写后 `__hash__` 自动失效);综合作业 `tinylog`(装饰器 + 上下文管理器 + 生成器三件套组合)跑通,装饰器第一版结构全错,两次重写后对齐,`/simplify` 7 处建议全部应用。
- **下次启动指令**:开始 Week 2 Day 8 —— asyncio 基础:事件循环、coroutine、Task、`asyncio.gather`;作业是并发请求 10 个网页并计时。

---

## 🚨 卡壳问题清单

<!-- 格式:[日期] 问题描述 — 状态(待解决 / 已解决在 YYYY-MM-DD) -->

- [2026-04-21] 生成器 + `with open(...)` 的文件生命周期:放在生成器里的 `with` 不由 caller 决定关闭时机,要靠生成器耗尽 / GC / `.close()`。**暂不解决,等实际遇到资源泄漏场景再处理**(正确规避:把 open 交给调用者,生成器只接 file-like)。
