# 当前断点

> 会话开始 Claude 只读这一个文件定位进度。
> 学习记录 / 学习清单 / 学习总结 都按需触发式读取。

---

## 📍 当前进度

- **当前阶段**:阶段一 · Python 高级语法补漏
- **当前周次**:Week 2(异步 + 工程化)
- **当前 Day**:Day 9 - async 深入(待开始)
- **状态**:Day 8 asyncio 基础完成 ✅。四件套(`async def` / `await` / `asyncio.run` / `gather`)讲透,"async def 调用不执行"踩了 2 次终于记住。作业并发爬 10 网页跑通,2.13s → 1.25s(共享 AsyncClient 优化,提速 41%)。**`asyncio.Task` / `create_task` 今天没讲**(用户选 B 路线提前结束),明天 Day 9 开头补 10 分钟再进入 Queue / Semaphore / wait_for / 取消。附带搞懂了 `*` / `**` 解包语法。
- **下次启动指令**:开始 Day 9 —— **先补 Day 8 遗留:`asyncio.Task` / `create_task`(为什么比 `gather` 更灵活,什么时候必须用)**,然后讲 `asyncio.Queue` / `Semaphore`(并发限流)/ `wait_for`(超时)/ `task.cancel()`(取消机制);作业是并发限流爬虫。

---

## 🚨 卡壳问题清单

<!-- 格式:[日期] 问题描述 — 状态(待解决 / 已解决在 YYYY-MM-DD) -->

- [2026-04-21] 生成器 + `with open(...)` 的文件生命周期:放在生成器里的 `with` 不由 caller 决定关闭时机,要靠生成器耗尽 / GC / `.close()`。**暂不解决,等实际遇到资源泄漏场景再处理**(正确规避:把 open 交给调用者,生成器只接 file-like)。
