# 当前断点

> 会话开始 Claude 只读这一个文件定位进度。
> 学习记录 / 学习清单 / 学习总结 都按需触发式读取。

---

## 📌 路线版本

**v2 · 17 周精简版**(2026-04-26 修订,从原 28 周脱产版精简而来)

- 起止:2026-04-18 → 2026-08 月中下旬
- 节奏:每天 6h,空窗脱产
- 求职定位:中小型 AI 公司 + 创业公司 + 传统行业 AI 部门(大厂短期不投)
- 砍:微调 + 评测、Reflection/ToT/Prompt 攻防、裸写 ReAct → 改读源码
- 加:Prometheus/Grafana/locust/LiteLLM/AutoGen/CrewAI/Milvus 简介
- OnCall 项目:基于小林 Coding 教程"二创 + 生产化补足",4 周完成

---

## 📍 当前进度

- **当前阶段**:阶段二 · 后端基础(Week 3 - 5)
- **当前周次**:Week 3(FastAPI)
- **当前 Day**:Day 18 - SSE 流式响应 ⭐(待开始)
- **状态**:Day 17 完整收官 ✅。在 Day 16 基础上叠 5 件事:`@app.middleware("http")` 洋葱模型(后注册的是外层)+ `@app.exception_handler(TodoNotFound)` 自定义业务异常 handler + `Exception` 兜底 handler + `CORSMiddleware` + 请求 ID/日志中间件(loguru `contextualize` 贯穿,`request.state.request_id` 挂载点)。**全天最反直觉认知**:`@app.exception_handler(Exception)` 在 FastAPI 被**特殊路由到最外层 `ServerErrorMiddleware`**(不是 ExceptionMiddleware),所以异常冲过所有用户中间件才被接住 —— 中间件里"`await call_next` 之后"的代码永不执行,500 case 的 `X-Request-ID` header 必须 handler 自己 `headers={...}` 加。配套规律:`await call_next(request)` 抛异常 = 后面所有代码跳过(Python 异常机制本质,中间件没特殊魔法)。**手测全过**:200(空 list,自生成 8 位 UUID 在 header)/ 404(`{"error":"todo_not_found",...}` body+header request_id 一致)/ 500(`{"error":"internal",...}` 自加 header)/ 自定义 X-Request-ID 原样返回。/simplify 9 处全应用(命名 / try/finally 单点日志 / `super().__init__` / `getattr` 防御 / `VALID_TOKEN` 常量 / `record` 局部变量去二次查找 等)。新增文件夹 `stage2_backend/day17_middleware_exception_cors/`。
- **下次启动指令**:开始 Day 18 —— SSE 流式响应 ⭐(关键验收点);学:SSE 协议格式 `text/event-stream`(`data: ...\n\n` 单向推流)+ `StreamingResponse` 配异步生成器 + 心跳 keep-alive + 与 WebSocket 选型对比(为什么 LLM 流式选 SSE 不选 WS);作业:模拟 LLM 逐字输出的 SSE 接口。

---

## 🚨 卡壳问题清单

<!-- 格式:[日期] 问题描述 — 状态(待解决 / 已解决在 YYYY-MM-DD) -->

- [2026-04-21] 生成器 + `with open(...)` 的文件生命周期:放在生成器里的 `with` 不由 caller 决定关闭时机,要靠生成器耗尽 / GC / `.close()`。**暂不解决,等实际遇到资源泄漏场景再处理**(正确规避:把 open 交给调用者,生成器只接 file-like)。
- [2026-04-27] `Semaphore` 包 retry vs 包单次的 tradeoff —— 这次选了简单写法(包 retry),但生产要"retry 在外 + jitter"。**已知不改,作业场景 OK**;OnCall 项目里实操 LiteLLM/MCP 重试时再回来调。
- [2026-05-03] Day 14 review 暴露的覆盖盲区:`check_url` 没 mock 测试(手写 fake AsyncClient 太繁琐 / `respx` 第三方库未引入)。**已加 `check_all` 行为测试覆盖了 Semaphore 语义**,但单个 check_url 的 200/404/500/异常分支仍只靠真实 httpbin 验证,网络抖动会让结论不稳定。Week 5 学 Docker / 监控 时,如果 OnCall 项目要稳定 CI,再回头加 `respx` 给 check_url 补 mock 测试。
