# 当前断点

> 会话开始 Claude 只读这一个文件定位进度。
> 学习记录 / 学习清单 / 学习总结 都按需触发式读取。

---

## 📌 路线版本

**v2 · 17 周精简版**(2026-04-26 修订,从原 28 周脱产版精简而来)

- 起止:2026-04-18 → 2026-08 月中下旬
- 节奏:每天 6h,空窗脱产
- 求职定位:中小型 AI 公司 + 创业公司 + 传统行业 AI 部门(大厂短期不投)
- 砍:微调 + 评测、Reflection/ToT/Prompt 攻防、裸写 ReAct → 改读源码
- 加:Prometheus/Grafana/locust/LiteLLM/AutoGen/CrewAI/Milvus 简介
- OnCall 项目:基于小林 Coding 教程"二创 + 生产化补足",4 周完成

---

## 📍 当前进度

- **当前阶段**:阶段一 · Python 高级语法补漏
- **当前周次**:Week 2(异步 + 工程化)
- **当前 Day**:Day 13 - 工程化(待开始)
- **状态**:Day 12 并发对比完成 ✅。GIL 本质 + threading/multiprocessing/asyncio 三方式选型讲清,作业 4 种方式 × 2 类任务 = 8 个组合全部跑通,**4 条反直觉认知数据印证**:(1)threading IO 0.51s / multiprocess IO 1.87s —— 进程启动开销让多进程跑 IO 反而慢 3.6 倍;(2)threading CPU 2.36s ≈ 串行 —— GIL 卡死多线程;(3)asyncio CPU 2.58s ≈ 串行 —— async def 没 await 等于同步;(4)multiprocess CPU 1.21s —— 真并行但 ≠ 1/N。/simplify 大手术 11 处:抽 `@contextmanager def timed` 干掉 8 段计时模板 / cpu_task_async 改 `return cpu_task(n)` 消除字节级重复 / 所有测试归到一个 async main / 常量提取 / 多 with 链式让 pool 创建销毁也算入 / range(N_IO) 替代 [2]*20 / imports 合并 / _async 后缀替代数字后缀 / 删 WHAT 注释 / label 对齐。**附带工程认知**:`asyncio.to_thread(func, *args)` 是 asyncio + threading 的正确合作姿势(老同步 SDK 混进 FastAPI 异步路由必用);"传函数对象不传调用结果"这条规律遍布 Python 所有"把函数交给别人执行"的 API。**附带工具配置**:`.claude/settings.json` 配了 PostToolUse hook,git commit 后自动 push。
- **下次启动指令**:开始 Day 13 —— 工程化收尾(`pyproject.toml` 项目元数据、`loguru` 结构化日志、`pytest` + `pytest-asyncio` 测试、异常体系设计);作业是给前面 Week 1-2 写过的工具(@retry / @timing / Config 类 / tinylog 等)加日志和测试,产出可 `uv build` 的本地 wheel。

---

## 🚨 卡壳问题清单

<!-- 格式:[日期] 问题描述 — 状态(待解决 / 已解决在 YYYY-MM-DD) -->

- [2026-04-21] 生成器 + `with open(...)` 的文件生命周期:放在生成器里的 `with` 不由 caller 决定关闭时机,要靠生成器耗尽 / GC / `.close()`。**暂不解决,等实际遇到资源泄漏场景再处理**(正确规避:把 open 交给调用者,生成器只接 file-like)。
- [2026-04-27] `Semaphore` 包 retry vs 包单次的 tradeoff —— 这次选了简单写法(包 retry),但生产要"retry 在外 + jitter"。**已知不改,作业场景 OK**;OnCall 项目里实操 LiteLLM/MCP 重试时再回来调。
