# 当前断点

> 会话开始 Claude 只读这一个文件定位进度。
> 学习记录 / 学习清单 / 学习总结 都按需触发式读取。

---

## 📌 路线版本

**v2 · 17 周精简版**(2026-04-26 修订,从原 28 周脱产版精简而来)

- 起止:2026-04-18 → 2026-08 月中下旬
- 节奏:每天 6h,空窗脱产
- 求职定位:中小型 AI 公司 + 创业公司 + 传统行业 AI 部门(大厂短期不投)
- 砍:微调 + 评测、Reflection/ToT/Prompt 攻防、裸写 ReAct → 改读源码
- 加:Prometheus/Grafana/locust/LiteLLM/AutoGen/CrewAI/Milvus 简介
- OnCall 项目:基于小林 Coding 教程"二创 + 生产化补足",4 周完成

---

## 📍 当前进度

- **当前阶段**:阶段一 · Python 高级语法补漏
- **当前周次**:Week 2(异步 + 工程化)
- **当前 Day**:Day 12 - 并发对比(待开始)
- **状态**:Day 11 类型 + Pydantic v2 完成 ✅。typing 现代写法(`list[int]` / `int \| None` / `Literal` / `TypedDict` / `Protocol`)+ Pydantic v2 三件套(`BaseModel` / `Field` / `ConfigDict`)+ 校验器(`field_validator` / `model_validator`)讲完。作业 LLM `Message` Schema(role / content / metadata / name + 跨字段 system 禁用 name + extra=forbid)4 个用例全部按预期通过。**头号坑**:`name: str \| None = Field(...)` 不加 default=None 仍当必填(Optional 类型 ≠ 可选字段);**第二坑**:Literal 误以为在 pydantic 里(实际 typing);**第三坑**:`field_validator` 实际不会触发(str_strip + min_length=1 已组合实现"不能全空白",写的 validator 是冗余)。/simplify 抓 7 处:model_config 移类顶部 / Field default 前置 / 函数名规则陈述句 / except 窄化为 ValidationError(防吞 bug)/ 循环变量 name 遮蔽字段名(HIGH)。
- **下次启动指令**:开始 Day 12 —— GIL 原理 + threading / multiprocessing / asyncio 适用场景;作业是同一 IO 任务和 CPU 任务三方式跑出耗时对比表,验证 4 条反直觉认知(GIL 卡死多线程跑 CPU、multiprocess IO 慢于 threading、async def 没 await 等于串行、multiprocess 加速 ≠ 1/N)。

---

## 🚨 卡壳问题清单

<!-- 格式:[日期] 问题描述 — 状态(待解决 / 已解决在 YYYY-MM-DD) -->

- [2026-04-21] 生成器 + `with open(...)` 的文件生命周期:放在生成器里的 `with` 不由 caller 决定关闭时机,要靠生成器耗尽 / GC / `.close()`。**暂不解决,等实际遇到资源泄漏场景再处理**(正确规避:把 open 交给调用者,生成器只接 file-like)。
- [2026-04-27] `Semaphore` 包 retry vs 包单次的 tradeoff —— 这次选了简单写法(包 retry),但生产要"retry 在外 + jitter"。**已知不改,作业场景 OK**;OnCall 项目里实操 LiteLLM/MCP 重试时再回来调。
