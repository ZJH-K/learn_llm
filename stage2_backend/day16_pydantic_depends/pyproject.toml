[project]
name = "day16-pydantic-depends"
version = "0.1.0"
description = "Day 16 — response_model + Depends + dependencies + responses"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn[standard]>=0.46.0",
]
